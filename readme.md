# yamashina

A small browser extension to replace English words with Japanese words whilst browsing. The vocabulary list comes from learned woards via the [Wanikani](http://wanikani.com/) API

Tested with Firefox, may or may not work with other browsers

## Development

This uses `TypeScript` and `Preact`, and is bundled using `Parcel`
